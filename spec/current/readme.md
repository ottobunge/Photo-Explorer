This folder has files that plan and track the progress of features as they are implemented.
Once we are tracking a file here it should be immediately removed from the backlog.
Once a feature is finished, it should be deleted from this folder and the general spec/ should be updated.
